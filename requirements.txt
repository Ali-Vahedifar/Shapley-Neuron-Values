# Versions the paper's CIFAR-100 campaign ran with (Python 3.10, CUDA 12.4).
torch==2.6.0
torchvision==0.21.0
numpy==2.2.6
pillow              # datasets.py (TinyImageNet loader); installed with torchvision anyway
psutil==5.9.0
tqdm==4.65.0
matplotlib
nvidia-ml-py        # pynvml: GPU utilisation, power, exclusivity checks in the cost ledger
pytest

# Optional: the code falls back without them.
scipy               # SNV: exact normal quantile for the MAB confidence bound
seaborn             # utils.py plotting style
